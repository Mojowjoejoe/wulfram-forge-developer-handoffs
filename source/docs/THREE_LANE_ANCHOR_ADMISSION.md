# Three-Lane Anchor admission

Admission: September 8, 2026. Twenty-one reviewed creative families, twenty-seven remaining toward 48. Private v106 catalog verification passes. V100 remains the accepted combined editor baseline.

Admission means the editor family contract has evidence. It does not certify game collision, combat effectiveness, competitive balance or novice-user acceptance.

| Family contract | Evidence and limits |
| --- | --- |
| Distinct spatial plan | Three courts, three offset mouths, transverse rear road and separate service yard; full topology/model-fitting regressions. Unlike Valley Pockets, these yards stay fixed and the plan provides three named exits per team. |
| Composition | Four sizes, explicit infrastructure/defense budgets, minimum or exact expanded counts, bounded fitting and radius rejection. The [guide](THREE_LANE_ANCHOR_GUIDE.md) supplies numeric limits and example footprints. |
| Deterministic recipe | Version 1 defaults and version 2 expanded roles; eight full golden expectations plus repeated generation in 288 cases. Original recipe template labels remain unchanged to retain golden output; displayed catalog/layout names lose the experimental suffix. |
| Terrain contract | Fixed geometry, preserved heights, oriented support, paired terrain checks and existing constraints. Gentle flat/valley/irregular fixtures pass; eight native steep cases reject without map/history mutation. No adaptive yard relocation. |
| Power and access | Original-model yard fitting, eight cells/team, stated provisional power budget, six sockets, sixteen corridor reservations and twelve pad routes. Editor sampling is not vehicle collision proof. |
| Usable documentation | Illustrated guide with role table, footprint examples, terrain/count limits, tradeoffs, socket binding, editing and authored reuse. Independently reviewed against source and golden outputs. |
| Variation | `outputs/anchor-variation-review-f2ms9z/report.json`: 288 successes, twelve seeds x four sizes x three terrain fixtures x two count modes. All 291 candidate/input hashes and manifest hash independently verified. Native `anchor-restart-native-VHZcdE` compares same-angle seeds 0/2 at every size on quiet texture copies; variation is modest but substantive. |
| Round trip and editing | v105 `anchor-restart-native-A2Ni37` covers GUI Preview/Apply/Undo and 24 terrain/count cases. `anchor-restart-native-d7S3yB` captures, exports/imports the authored library through MCP, reuses 78 buildings and 16 corridors on a larger map, and explicitly reopens the reused map and exact library in a fresh process. Library history is separate from map Undo. |

All report paths above are relative to `outputs/` unless already prefixed. Full hashes, retained failed harness runs and exact scopes are in the [design evidence](THREE_LANE_ANCHOR_DESIGN.md). Independent reviews found no remaining actionable source, artifact or guide issue before promotion. Source-targeted checks pass in `outputs/three-lane-anchor-admission-tests-rerun.log` (15 tests), `three-lane-anchor-admission-typecheck.log` and `three-lane-anchor-admission-sync.log`. The first admission test run retained old catalog-count expectations; those assertions now expect 24 cards representing 21 families.

MCP impact: existing `generate_base_layout`, authored capture/library/reuse and inspection operations remain the interface. No schema, native allowlist or geometry changes accompany catalog promotion. V105 remains compatible with expanded recipes; v106 presents the family in Creative. No commit, push or public distribution is part of this admission.

## Private v106 catalog verification

Executable: `D:/WulframForgeBuilds/three-lane-anchor-v106/WulframForge.exe`, SHA-256 `186e05011a80fb2e0e2fc0de38abe899568717a518d64989da4d34bac07e730b`. Build log: `outputs/three-lane-anchor-v106-build.log`. Archive: `dist/desktop/WulframForge-0.7.0-v106-win-x64-self-contained.zip`.

`outputs/anchor-restart-native-hnvLGP/report.json` passes `--library --admitted --expanded --authored --authored-library`: Creative collection search, four size counts and diagrams, fixed/adaptive filtering, GUI expanded Preview/Apply/Undo, whole generated-map reopening, authored library export/remove/preview-import/import, larger-map translation and a further fresh-process reopen preserving both library and reused map. Renderer errors are empty. The guide uses the current Creative card screenshot. This is scoped catalog/lifecycle acceptance; the v105 24-case native matrix is not claimed as rerun here. The accepted installer and launcher selection are unchanged.

Independent final review verified all seven build/artifact hashes and the current guide screenshot; no actionable finding remains. Full source suite `outputs/three-lane-anchor-v106-all-tests.log` passes 475 tests total: 474 passed, one existing skip, zero failures. These source checks do not replace combined native acceptance.
