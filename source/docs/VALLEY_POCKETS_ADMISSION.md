# Valley Pockets admission — twentieth reviewed family

Independent review supports admission under the eight editor-quality gates. The source catalog now lists one Valley Pockets family in Creative; the promoted v100 native presentation passed. Earlier v99 evidence covers the Experimental presentation. No recipe geometry or V1/V2 fixtures changed during promotion.

| Gate | Evidence | Scope |
| --- | --- | --- |
| Distinct plan | VALLEY_POCKETS_DESIGN.md and independent schematic review | Alternating side yards around an open spine; differs from shared court, loop and stepped-chain families. |
| Composition | Guide role budgets, original model contract, bounds, count fitter | Four sizes; defaults10/15/20/30; infeasible exact counts reject. |
| Determinism | V1 and V2 reference fixtures; versioned portable reconstruction | Old defaults unchanged, fixed favorites retain fitted geometry. |
| Terrain | Source terrain tests and native BG5xx0 matrix | 24 native size/terrain/count cases, one seed, same-map reuse at35degrees; no hidden flattening. |
| Power/access | Shared validators, native matrix and restart pad audits | Conservative editor power,96u service criterion, exact pad associations; not game collision proof. |
| Documentation | VALLEY_POCKETS_GUIDE.md; 4jo5BA service-yard image, tZFjI1 close/overhead views | Reviewed roles, bounds, numeric limits and original-model relationships. |
| Variation | Four seed0/3/11 schematic sheets; terrain seed tests | Independent review found meaningful station/setback/branch/side-order variation. |
| Round trips/editing | q9TyDf native library; restart and WNhvbL portability; 96 source portability cases | Preview immutability, Apply/Undo, JSON/ZIP, library export/import/cancel/restart, cross-map coordinates and retained buildings. |

Receipt roots are `tools/mcp/MapEditerMCP/outputs/mcp-native-test-q9TyDf/` and `mcp-native-test-WNhvbL/`. Full paths, hashes and intermediate corrections are recorded in VALLEY_POCKETS_DESIGN.md. The tested v99 EXE SHA256 is a63d9a434cb0686ae26c9261f8ea2f50ce5fefa1222975089500603bb34ffef0.

There are now 20 reviewed families in source, across23Creative cards;28more families remain toward48. Combined baseline/installer remainv96. This admission does not close competitive balance, in-game pad/collision, novice acceptance, whole-roadmap completion or public-release gates.


Private v100 build completed: `D:/WulframForgeBuilds/valley-pockets-admitted-v100/WulframForge.exe`, SHA256 `3573ad846932d4a1aa1f184163b3f4ca2bf4a064af85b62853cc6b1f2c731291`. Native promoted-card verification passed: `tools/mcp/MapEditerMCP/outputs/mcp-native-test-iCzwhR/report.json`. Its report SHA256 is `702ea45231064f5dbe6faeca68573aed0a87cef91a85641006a289ca2a489e7c`; the adjacent `valley-pockets-artifact-audit.json` passed with 19 artifacts. The harness uses Creative library search because the card is beyond the first page. Combined runner review passed after explicit child receipt checks, parent linkage, exact 24-case matrix scope, artifact hashes and final fixture rechecks were added. Full 21-step v100 execution started with `--valley-pockets`; see `outputs/product-baseline-v100.log`. Combined acceptance remains pending until the terminal report and independent audit pass.


Combined attempt `outputs/product-baseline-UwEW5I/report.json` exposed stale Experimental/category and Creative dropdown expectations after promotion. Both assertions were updated to the reviewed catalog. Full source recheck `outputs/v100-source-recheck.log` passed 451 tests: 450 passed, one existing skip, zero failures. The first aggregate remains unaccepted; its remaining independent native suites continue before a corrected combined run. No runtime code changed in these corrections.

The first combined run also exposed an authored-library harness assertion using `.project` on a helper that already returns the project. That assertion was corrected without changing product code; the separate Valley helper keeps its own wrapper contract. Creative acceptance now pins the EXE, base JSON/ZIP and three terrain-trial ZIP inputs before native launch and rechecks them before PASS; the runner and auditor verify the same paths/hashes. Fresh native execution is still required for these harness corrections.


### Promoted restart and terrain acceptance

The first combined attempt `outputs/product-baseline-UwEW5I/report.json` is terminal failed; it is not accepted. Its four Valley children all passed on the same v100 EXE: native `mcp-native-test-DE0Q6G/report.json`, adjacent 19-artifact audit, `valley-restart-Gigrgs/valley-restart-report.json`, and `valley-matrix-wzWVyJ/valley-matrix-report.json`. These paths are beneath `tools/mcp/MapEditerMCP/outputs/`. Restart retains the exact library and checks cross-map GUI reuse, geometry, access, Apply/Undo and unchanged camera-only map state. The matrix covers 24 exact size/terrain/count combinations; the aggregate pins its 48 fixture/copy artifacts and restart applied copy.

The three failing first-run steps were source catalog expectation, Creative dropdown expectation and the authored-library snapshot assertion; fixes are harness/test-only. A fresh full combined run is recorded in `outputs/product-baseline-v100-corrected.log`. It must finish and pass independent aggregate auditing before v100 replaces v96 as the combined baseline.


Corrected aggregate `outputs/product-baseline-iZYLbw/report.json` finished with 20/21 stages passing. Authored restart alone failed on a stale CDP DOM node. The harness now waits for its file input and re-resolves only on explicit missing-node errors, never on ambiguous timeouts. Original failure is retained as `mcp-native-test-ngDKXe/library-restart-first-failure.json`; the targeted corrected restart passed in `outputs/v100-authored-restart-corrected.log`, retaining complete packages and GUI entries. This has not promoted the failed aggregate; independent review and complete corrected acceptance remain pending.

Independent review accepted the bounded stale-node fix and verified the corrected restart receipt, parent linkage and exact v100 executable. A fresh 21-stage run with all harness corrections is now recorded in `outputs/product-baseline-v100-final.log`. Prior failed aggregates remain unchanged. Runtime source remains unchanged during verification.


Final v100 combined acceptance: [COMBINED_BASELINE_V100.md](COMBINED_BASELINE_V100.md). Fresh MuzOAW passes all 21 stages and both automated and independent final audits. This supersedes earlier pending combined-verification notes; historical failed attempts remain retained. Installer acceptance remains v96.
